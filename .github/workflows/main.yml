<<<<<<< HEAD:.github/workflows/main.yml
# CI on PRODUCTION enviroment - main branch -> tisamed-oradea.ro
=======
# CI on TEST enviroment - test branch -> test.tisamed-oradea.ro
>>>>>>> 2cd5aa77f6bc4bdf1fe1aebfcb26a8ce4c039c96:.github/workflows/test.yml

name: CI
# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  cd:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest]
        node: [14]

    steps:
      - name: Checkout
        uses: actions/checkout@master

      - name: Setup node env
        uses: actions/setup-node@v2.1.2
        with:
          node-version: ${{ matrix.node }}

      - name: deploy
        uses: SamKirkland/FTP-Deploy-Action@4.0.0
        with:
          server: ftp.tisamed-oradea.ro
          username: deploy@test.tisamed-oradea.ro
          password: ${{secrets.FTP_PASSWORD_TEST}}
          local-dir: ./
